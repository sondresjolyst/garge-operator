namespace garge_operator.Constants
{
    /// <summary>
    /// Switch device types recognized by the operator. Only actionable types are automated.
    /// </summary>
    public static class SwitchTypes
    {
        public const string Socket = "socket";
    }
}
